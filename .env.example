# Application Environment Variables
# NEXT_PUBLIC_... variables if needed for frontend

# Note: As a purely Client-Side Rendered (CSR) app, do not place sensitive secrets in .env that would be bundled into the build.
